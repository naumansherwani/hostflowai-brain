{
  "name": "hostflowai-brain",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd backend && node ./build.mjs",
    "start": "node --enable-source-maps ./backend/dist/index.mjs",
    "deploy": "bash ./deploy.sh"
  }
}
